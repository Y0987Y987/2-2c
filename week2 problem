#include <stdio.h>

int mul99(a, b, s){
	
	for (a=1;a<=9;a++){
		for (b=1;b<=9;b++){
			s = a * b;
			printf("%d*%d=%d\n", a, b, s);
		}
		printf("\n");
	}
	return 0;
}

int main () {
	printf("%d\n", mul99(0,0,0));
}
// 最後一行跑出0
